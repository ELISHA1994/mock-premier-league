build:
  docker:
    web: Dockerfile
run:
  web: docker-compose up --build
setup:
  addons:
    - plan: heroku-redis
  config:
    PROD_DB_URL: mongodb+srv://todoappDB:todoappdb@cluster0.sjh2l.mongodb.net/mock-premier-league?retryWrites=true&w=majority

